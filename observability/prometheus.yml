global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'echo-server-go'
    static_configs:
      - targets: ['echo-server-go:8080']

rule_files:
  - "alert_rules.yml"