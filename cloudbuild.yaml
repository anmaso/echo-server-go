steps:
  - name: build
    script: |
      docker build -t europe-southwest1-docker.pkg.dev/demouv2/uv/echo:$COMMIT_SHA .
images:
  - europe-southwest1-docker.pkg.dev/demouv2/uv/echo:$COMMIT_SHA
options:
  logging: CLOUD_LOGGING_ONLY
